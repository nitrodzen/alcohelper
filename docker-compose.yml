services:
  postgres:
    image: postgres:17-alpine
    container_name: alco-helper-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: alco_helper
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d alco_helper"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
